spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/todo_db?currentSchema=public
    username: postgres
    password: 0909
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
